
DROP DATABASE IF EXISTS videodaw;
CREATE DATABASE videodaw;
USE videodaw;

CREATE TABLE generos (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(50) NOT NULL UNIQUE
);


-- Tabla de clientes
CREATE TABLE clientes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    dni VARCHAR(20) NOT NULL UNIQUE,
    nombre VARCHAR(100),
    direccion VARCHAR(150),
    fechaNacimiento DATE,
    numSocio VARCHAR(50) UNIQUE,
    fechaBaja DATE
);

CREATE TABLE vinilos (
    id INT AUTO_INCREMENT PRIMARY KEY,
    codigo VARCHAR(50) NOT NULL UNIQUE,
    titulo VARCHAR(100),
    genero INT,
    isAlquilado BOOLEAN,

    FOREIGN KEY (genero) REFERENCES generos(id)
);

CREATE TABLE alquileres (
    id INT AUTO_INCREMENT PRIMARY KEY,
    cliente_id INT,
    vinilo_id INT,
    fechaAlquiler DATE,
    fechaDevolucion DATE,

    FOREIGN KEY (cliente_id) REFERENCES clientes(id),
    FOREIGN KEY (vinilo_id) REFERENCES vinilos(id)
);

INSERT INTO generos (nombre) VALUES
('ROCK'),
('HEAVY METAL'),
('RAP'),
('NU METAL'),
('HIP HOP');
	
INSERT INTO clientes (dni, nombre, direccion, fechaNacimiento, numSocio, fechaBaja) VALUES
('12345678A', 'Juan Perez', 'Calle A', '2000-05-10', 'SOC001', NULL),
('87654321B', 'Maria Lopez', 'Calle B', '1998-03-20', 'SOC002', NULL);


INSERT INTO vinilos (codigo, titulo, genero, isAlquilado) VALUES
('VIN001', 'Dark Side of the Moon', 1, false),
('VIN002', 'Illmatic ', 3, false),
('VIN003', 'The Eminem Show', 3, false);

SELECT * FROM clientes;
SELECT * FROM vinilos;
SELECT * FROM generos;
SELECT * FROM generos ORder by id asc;
SELECT * FROM alquileres;
